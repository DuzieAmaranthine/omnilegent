export const AddBook = ({ open }) => {

  return (
    <div className="add-holder">
      <button className="add-button" onClick={open}> + Add Something New To Your Shelf + </button>
    </div>
  );
};
